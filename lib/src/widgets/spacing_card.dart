import 'package:flutter/material.dart';

class SpacingCard extends StatelessWidget {
  const SpacingCard({
    super.key,
  });
  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
